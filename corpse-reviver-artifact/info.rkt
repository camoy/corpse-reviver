#lang info

;; General

(define collection "corpse-reviver-artifact")
(define version "0.0")
(define scribblings
  '(("scribblings/corpse-reviver-artifact.scrbl" (no-search))))

(define deps '("base"))
(define build-deps '())
